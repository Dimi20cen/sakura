import type { UIConfig } from "../types.ts";

export const handConfig: UIConfig["hand"] = {
    devConfirmationCardWidth: 120,
    devConfirmationButtonGap: 10,
    devConfirmationButtonHeight: 80,
};
